cmake_minimum_required(VERSION 3.25)
project(dusk_gz_mod LANGUAGES CXX)
if(PROJECT_IS_TOP_LEVEL)
    set(DUSK_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../dusklight-upstream" CACHE PATH "Upstream TwilitRealm/dusklight checkout")
    if(NOT EXISTS "${DUSK_DIR}/sdk/CMakeLists.txt")
        message(FATAL_ERROR "Set DUSK_DIR to an upstream Dusklight checkout with submodules initialized.")
    endif()
    file(READ "${DUSK_DIR}/src/d/d_cc_uty.cpp" source_text)
    string(REPLACE "\r\n" "\n" source_text "${source_text}")
    string(SHA256 source_hash "${source_text}")
    if(NOT source_hash STREQUAL "33b6e2b58c41758fcc92d535fa1a89f789e8d26b737b8055edfdb48b4f4f1fc1")
     message(FATAL_ERROR "Upstream src/d/d_cc_uty.cpp changed. Review/regenerate its GZ adapter, preserving upstream QoL changes, then update its source hash.")
    endif()
    file(READ "${DUSK_DIR}/src/d/actor/d_a_alink_hook.inc" source_text)
    string(REPLACE "\r\n" "\n" source_text "${source_text}")
    string(SHA256 source_hash "${source_text}")
    if(NOT source_hash STREQUAL "52bbbd8a8723d0a9efd6752e3813d36afa350296745d8f6970e337ad07b3bf8a")
     message(FATAL_ERROR "Upstream src/d/actor/d_a_alink_hook.inc changed. Review/regenerate its GZ adapter, preserving upstream QoL changes, then update its source hash.")
    endif()
    execute_process(COMMAND git -C "${DUSK_DIR}" rev-parse HEAD OUTPUT_VARIABLE dusk_revision OUTPUT_STRIP_TRAILING_WHITESPACE ERROR_QUIET)
    file(WRITE "${CMAKE_BINARY_DIR}/build-info.txt" "Dusk SDK: ${DUSK_DIR}\nRevision: ${dusk_revision}\nTPGZ: f8a29de6e3e20664e6b16ae37caf066f5922c64d\n")
    file(READ "${DUSK_DIR}/src/dusk/config_var.hpp" movement_source)
    string(REPLACE "\r\n" "\n" movement_source "${movement_source}")
    string(SHA256 movement_hash "${movement_source}")
    if(NOT movement_hash STREQUAL "ad545cf9f86e36f8cebcb82d400e3dd54ff385d4004c0adbb589da04ee80cb50")
     message(FATAL_ERROR "Review the fast-movement adapter: upstream src/dusk/config_var.hpp changed.")
    endif()
    file(READ "${DUSK_DIR}/src/d/actor/d_a_alink.cpp" movement_source)
    string(REPLACE "\r\n" "\n" movement_source "${movement_source}")
    string(SHA256 movement_hash "${movement_source}")
    if(NOT movement_hash STREQUAL "603137b76bbfb0c45e67a86d8bdf1edf1e1c5c5caa55e2a0fb32f43afec03669")
     message(FATAL_ERROR "Review the fast-movement adapter: upstream src/d/actor/d_a_alink.cpp changed.")
    endif()
    file(READ "${DUSK_DIR}/src/d/actor/d_a_alink_swim.inc" movement_source)
    string(REPLACE "\r\n" "\n" movement_source "${movement_source}")
    string(SHA256 movement_hash "${movement_source}")
    if(NOT movement_hash STREQUAL "f3e39d0fc5844ea391af3a5fbf84951fb66d27716470b16fbae7ae6f0c202ae3")
     message(FATAL_ERROR "Review the fast-movement adapter: upstream src/d/actor/d_a_alink_swim.inc changed.")
    endif()
    file(READ "${DUSK_DIR}/src/d/actor/d_a_alink_wolf.inc" movement_source)
    string(REPLACE "\r\n" "\n" movement_source "${movement_source}")
    string(SHA256 movement_hash "${movement_source}")
    if(NOT movement_hash STREQUAL "b0c094b0c95144d7c5f89bc1d35d63fcde80f1f032a7772670a8142eb4dc9d8d")
     message(FATAL_ERROR "Review the fast-movement adapter: upstream src/d/actor/d_a_alink_wolf.inc changed.")
    endif()
    file(READ "${DUSK_DIR}/src/d/actor/d_a_alink_cut.inc" projection_source)
    string(REPLACE "\r\n" "\n" projection_source "${projection_source}")
    string(SHA256 projection_hash "${projection_source}")
    if(NOT projection_hash STREQUAL "08675292bef7a1243ba76744e082094a2451d4567090f5a77fe156b6389eaa10")
     message(FATAL_ERROR "Review native projection state restoration: upstream src/d/actor/d_a_alink_cut.inc changed.")
    endif()
    add_subdirectory("${DUSK_DIR}/sdk" dusk-sdk EXCLUDE_FROM_ALL)
else()
    # In-tree builds use the host SDK, ABI targets and bundled-mod installer.
    set(DUSK_DIR "${CMAKE_SOURCE_DIR}")
endif()
add_mod(dusk_gz FEATURES game webgpu
    SOURCES src/speedrun_guard.cpp src/projection.cpp src/tunic.cpp src/link_movement.cpp src/transform_indicator.cpp src/combo_menu.cpp src/flag_log.cpp src/flag_records.cpp src/triggers.cpp src/collision.cpp src/mod.cpp src/actor_tools.cpp src/scene.cpp src/core.cpp src/ui.cpp src/cheats.cpp src/input.cpp
            src/enemy_damage.cpp src/item_lifetime.cpp src/clawshot.cpp src/inventory.cpp src/item_wheel.cpp src/reload.cpp src/collectibles.cpp src/flags.cpp src/rng.cpp src/camera.cpp src/warping.cpp src/practice.cpp src/gz_font.cpp src/foreground.cpp src/gz_menu.cpp src/overlays.cpp src/input_viewer.cpp src/presentation.cpp src/link_debug.cpp src/loading.cpp src/link_tools.cpp src/checkers.cpp
    MOD_JSON mod.json RES_DIR res BUNDLE)
# Keep local build paths out of the distributed DLL debug-directory record.
if(MSVC)
 target_link_options(dusk_gz PRIVATE "/PDBALTPATH:dusk_gz.pdb")
endif()
set_source_files_properties(src/link_movement.cpp src/input.cpp PROPERTIES INCLUDE_DIRECTORIES "${DUSK_DIR}/src")
if(PROJECT_IS_TOP_LEVEL)
 include(CTest)
endif()
if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
    add_executable(input_logic_tests tests/input_logic_tests.cpp)
    target_compile_features(input_logic_tests PRIVATE cxx_std_20)
    target_include_directories(input_logic_tests PRIVATE src)
    add_test(NAME input_logic COMMAND input_logic_tests)
    add_executable(behavior_tests tests/behavior_tests.cpp)
    target_compile_features(behavior_tests PRIVATE cxx_std_20)
    target_include_directories(behavior_tests PRIVATE src)
    add_test(NAME behavior COMMAND behavior_tests)
endif()

if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(practice_layout_tests tests/practice_layout_tests.cpp)
 target_link_libraries(practice_layout_tests PRIVATE dusklight_game_abi_headers)
 target_compile_features(practice_layout_tests PRIVATE cxx_std_20)
 if(MSVC)
  target_compile_options(practice_layout_tests PRIVATE /FIglobal.h)
 else()
  target_compile_options(practice_layout_tests PRIVATE -include global.h)
 endif()
 add_test(NAME practice_layout COMMAND practice_layout_tests "${CMAKE_CURRENT_SOURCE_DIR}/res/practice")
endif()
if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(font_data_tests tests/font_data_tests.cpp)
 target_compile_features(font_data_tests PRIVATE cxx_std_20)
 target_include_directories(font_data_tests PRIVATE src)
 add_test(NAME font_data COMMAND font_data_tests "${CMAKE_CURRENT_SOURCE_DIR}/res/fonts/consola.fnt")
endif()
if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(timer_tests tests/timer_tests.cpp)
 target_compile_features(timer_tests PRIVATE cxx_std_20)
 target_include_directories(timer_tests PRIVATE src)
 add_test(NAME timers COMMAND timer_tests)
endif()
if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(practice_callback_tests tests/practice_callback_tests.cpp)
 target_compile_features(practice_callback_tests PRIVATE cxx_std_20)
 target_include_directories(practice_callback_tests PRIVATE src)
 add_test(NAME practice_callbacks COMMAND practice_callback_tests)
 add_executable(menu_logic_tests tests/menu_logic_tests.cpp)
 target_compile_features(menu_logic_tests PRIVATE cxx_std_20)
 target_include_directories(menu_logic_tests PRIVATE src)
 add_test(NAME menu_logic COMMAND menu_logic_tests)
endif()

if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(gorge_feedback_tests tests/gorge_feedback_tests.cpp)
 target_compile_features(gorge_feedback_tests PRIVATE cxx_std_20)
 target_include_directories(gorge_feedback_tests PRIVATE src)
 add_test(NAME gorge_feedback COMMAND gorge_feedback_tests)
endif()

if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(scoped_restore_tests tests/scoped_restore_tests.cpp)
 target_compile_features(scoped_restore_tests PRIVATE cxx_std_20)
 target_include_directories(scoped_restore_tests PRIVATE src)
 add_test(NAME scoped_restore COMMAND scoped_restore_tests)
endif()


if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(practice_destination_tests tests/practice_destination_tests.cpp)
 target_link_libraries(practice_destination_tests PRIVATE dusklight_game_abi_headers)
 target_include_directories(practice_destination_tests PRIVATE src)
 target_compile_features(practice_destination_tests PRIVATE cxx_std_20)
 if(MSVC)
  target_compile_options(practice_destination_tests PRIVATE /FIglobal.h)
 else()
  target_compile_options(practice_destination_tests PRIVATE -include global.h)
 endif()
 add_test(NAME practice_destination COMMAND practice_destination_tests)
endif()

if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(presentation_layout_tests tests/presentation_layout_tests.cpp)
 target_compile_features(presentation_layout_tests PRIVATE cxx_std_20)
 target_include_directories(presentation_layout_tests PRIVATE src)
 add_test(NAME presentation_layout COMMAND presentation_layout_tests)
endif()

if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(hook_guard_tests tests/hook_guard_tests.cpp)
 target_compile_features(hook_guard_tests PRIVATE cxx_std_20)
 target_include_directories(hook_guard_tests PRIVATE src)
 add_test(NAME hook_guard COMMAND hook_guard_tests)
endif()

if(PROJECT_IS_TOP_LEVEL AND BUILD_TESTING)
 add_executable(oxygen_init_tests tests/oxygen_init_tests.cpp)
 target_compile_features(oxygen_init_tests PRIVATE cxx_std_20)
 target_include_directories(oxygen_init_tests PRIVATE src)
 add_test(NAME oxygen_init COMMAND oxygen_init_tests)
endif()
